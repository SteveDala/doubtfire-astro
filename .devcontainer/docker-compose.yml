services:
  astro-dev:
    build:
      context: ../
      dockerfile: Dockerfile
    ports:
      - "4321:4321"  # or whatever port your dev server uses
    volumes:
      - ..:/site
      - astro-node_modules:/site/node_modules
volumes:
  astro-node_modules: